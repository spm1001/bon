# Accent audit — Sameer-tissue in the review and open spines (2026-08-30)

Input for bon-hedatu (the four-rites personal-accent extraction). Compiled from the two adversarial essayeur rounds run against the lane-era rewrite (bon-veleru, bon-vatoge) the night it shipped in suite 1.82.0 — each line below was found by a fresh-context verifier walking the rite as a non-Sameer reader. Line numbers are as of commit a4393db and will drift; the quoted phrases are the durable keys.

## skills/review — spine tissue (outside the socket)

- pyramid-format.md "Cross-cutting Work" ranked list: "top 4 — **Sameer's ranking**" — operator desire data, no fallback for a first run or another operator.
- pyramid-format.md exemplar paragraphs: canonical exemplars at `~/scratch/estate-pyramid-2026-08-01.md` and `~/notes/raw/claude/bon-audit-*/pyramid-final-snapshot.md` — the spec defers to files that exist only on Sameer's estate (a "stands alone off-estate" clause was added 2026-08-30, but the authority still lives there).
- pyramid-format.md jobs assignment: "agree placement with **Sameer**" — spine step, personal name.
- pyramid-format.md stockpot loop: canonical map `~/notes/practices/stockpot-map.html` — required for any knowledge-work group render, nonexistent off-estate, no fallback.
- SKILL.md Phase 1 STOP: "his preferred shape", "His wordings apply verbatim".
- SKILL.md Phase 5: archive path `~/notes/raw/claude/bon-audit-{date}/`.
- SKILL.md frontmatter: "Toolmaking reconciliation" as a trigger phrase.
- Alignment block + queue population: guarded (absent-vs-unreachable split, skip-named socket) as of 1.82.0, but the lane names, the `& Toolmaking` project name and `~/.claude/loop.md` pointer are his furniture in core text.

## skills/open — §4 Toolmaking Compass (wholesale personal half)

- The whole step is his: "Sameer's session-boundary guide", the `& Toolmaking` project name, `~/.claude/loop.md` semantics pointer (dangles off-estate), "his P1 marks in With Sameer flag the staying conversations", the nudge's "his move — don't draw it down".
- Interim guard added 2026-08-30 (bon-vatoge): the not-found branch discriminates the book via `accomplis whoami` — loud on his book, silent on a foreign one (honours hedatu law 1, never nag empty slots; live precedent: Stef, 2026-08-11, recorded in bon-hedatu's --why).
- **Open watch cell**: a teammate whose own book legitimately carries a "& Toolmaking" project sails BOTH gates (CLI present, project found) and gets their queue rendered under his lane semantics, silently. No live instance. The whoami guard does not cover it — only the full accent extraction does.

## skills/close — noted in passing (not audited this round)

- The tap reads "Sameer's dispatch queue" and writes `& Toolmaking` lines (propose-and-add). Lane placement gap filed separately as bon-hofoju.

Provenance: veleru essayeur rounds 1–2 and vatoge essayeur rounds 1–2, 2026-08-30 evening; verdicts summarised in the two cards' done_notes; session handoff `handoffs/2026-08-30-2010-fb49cab3.md`.
