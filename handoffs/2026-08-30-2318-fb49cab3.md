# Handoff — 2026-08-30

session_id: fb49cab3-94db-4e47-9d33-1cb4607c695f
purpose: The Common Core five land as one session and publish before the mit rite fork — sync, sweep, baton, nudge, personal-half split (bon-guritu, bon-supuko, bon-jeweke, bon-nalube, bon-hedatu)
items: bon-guritu, bon-supuko, bon-jeweke, bon-nalube, bon-hedatu
format: fond-v1

## For the next Claude

### Done
- **All five Common Core cards closed and published as suite 1.83.0** (assemble run 33338718077 read green independently; both scope pins — user AND the /home/modha project pin — verified at 1.83.0 after the project pin stuck at 1.82.0 exactly as the previous close warned). The seam rule held: bon's five published before the mit plugin's rite fork is cut.
- **bon-guritu closed.** JSONL write-verbs own sync (`src/bon/gitsync.py`): fetch → rebase → item-grain merge → write → commit → push, self-gating on tracked items.jsonl + upstream + no `.bon/sync` off-marker (`~/notes` got the marker; notes-sync owns its git writes). Three essayeur rounds: REFUTED (while-apart same-item edits silently lost to union+newest-wins) → repaired with `resolve_union_artifacts` (newest wins, displaced versions preserved in committed `.bon/sync-conflicts.jsonl`, loud per-id warning, standing cue on every clone) → REFUTED again (a reviewed-and-deleted sidecar could never stage, wedging sync) → repaired (tracked-or-existing board paths, post-rebase cue) → PASS. 22 tests incl. two-clone fixtures.
- **bon-supuko closed.** The sweep replaces latest-wins: LEDGER.md `- [ ]` processed-markers (HANDOFF-CONTRACT v7), close appends in the same change, open-context emits UNPROCESSED= lines and the open skill sweeps all, ticking after processing. Essayeur REFUTED the rollout-week mixed-version interleave (an un-ledgered handoff beside one unticked line became permanently unreachable) → repaired with the un-ledgered net + drift warning + LEDGER excluded from HANDOFF= ranking + global stash excluded → PASS, with the adoption-backfill rule added (a fresh ledger beside 84 historical files must backfill them ticked — this handoff's own ledger, created tonight, is the first live use).
- **bon-jeweke closed.** The baton follows the ticket: closes stamp `items:` frontmatter (WORKED-only — closed/stepped/materially advanced, never merely filed — one physical line); `bon work` surfaces the newest handoff citing the drawn item via resolver-semantics dirs (upward floor + depth≤4 room glob, foreign .git/.bon boundaries excluded). Essayeur REFUTED fourfold (walk budget tripped on ~/.claude reaching only vendored plugin-cache handoffs; sentinel ranking; 800-byte head; "or filed" semantics) → repaired → WATCH with two drift residuals (prose-eating continuation joiner; mtime-not-provenance on shared boards) → both repaired same session (indented-only continuations; git commit date outranks flattened mtime). 11 tests. Adoption note: this handoff's `items:` line is the estate's first — the baton goes live as v7 closes accumulate.
- **bon-nalube closed.** File-it-then-do-it ritualised at close's filing step, with the essayeur's riders folded (knocked-out items get `bon done`'d so the split reaches the board; user demotions at review stay filed). PASS.
- **bon-hedatu closed.** All four rites read `~/.claude/mit-accent.md` at designated points, keyed by the SETTLED ids — `open.personal`, `close.personal`, `plan.personal`, `review.personal` (Sameer's own naming call, 2026-08-30 ~23:10, relayed live mid-session: ownership-named, NOT "-local"; his earlier steer had already banned the four metaphor names from core and format). Four laws in `docs/ACCENT.md`, format inlined at close's cultivation step because docs/ don't ship in the plugin. His halves extracted verbatim to `~/.claude/mit-accent.md` (spm1001/.claude c982659→f705d8c — his pet names survive only as his own prose inside). Essayeur verdict WATCH with all three --done clauses holding: no-accent machine runs all four rites complete and silent (fixture-verified), his machine keeps pre-split behaviour, the 2026-08-11 teammate-nag watch cell structurally cured. Every act/watch folded same session, including de-personalising the always-on instructions.md shard and generalising "Up Next" out of the pyramid spec.
- Full suite at close: 864 passed, 18 skipped (the opt-in Dolt slice). Ten content commits plus repairs, each citing its card; everything pushed.
- **Two publishes, deliberately named:** 1.83.0 carried the five cards (the seam publish); 1.83.1 carried one late steer — Sameer momentarily read "open.personal" as a personal /open beside a team one, so every first mention of a `rite.personal` id now glosses that it names a point INSIDE the single rite, never a sibling command. A cold teammate would have made the same read.

### Reflection
- **The essayeurs earned their keep tonight: three REFUTED verdicts on first rounds, all real.** Guritu's offline-collision hole was the primary path for the card's own motivating scenario (laptops that sleep); jeweke's walk-budget kill was demonstrated live on ~/.claude; supuko's rollout-week interleave was exactly the deployment week ahead. Every repair got its own adversarial round before the card closed — the veleru handoff's "no post-repair essayeur round was run" lesson, applied five times.
- **Two operator steers landed mid-session and both were naming steers** — first "no pet names in core or format", then the settled `rite.personal` ids. The socket-id form absorbed both without touching behaviour: rename keys, re-key his accent, re-grep. Naming-by-ownership beat my position-keyed interim ids (`open.pre-pick`) because it answers the reader's actual question — whose content is this? — rather than where it runs.
- One instrument note: a `grep | head; echo exit=$?` pipeline consumed its own exit status mid-session (the traps row, live) — re-probed cleanly before relying on the null.

### Uncertain
- Whether the hedatu socket-rename chain has had truly fresh eyes: the essayeur's walk mostly predated 6291ced (it re-walked the rename but its final consolidated verdict interleaved with my residue sweep, and the last rename to `rite.personal` landed after it finished). Coherence is verified by grep and construction (zero old ids anywhere; hook → `## <rite>.personal` → accent headings), not by a cold reader. The next real /open on Sameer's machine is the live test.
- Whether a teammate's Claude, reading `review.personal` prose that names lanes generically ("the workable lane"), renders lane names sensibly at a ceremony. No live instance until the mit fork ships.

### Risks
- **Nothing behavioural in the skill text was verifiable in this session** — skill files are session-cached; every verdict above is cold-read essayeur plus mechanical fixtures. Live verification belongs to the next real /open, /close, and ceremony. Before judging any misbehaviour, confirm the loaded plugin version reads 1.83.0 (both scopes checked tonight, but a third scope pin elsewhere would replay the stale-text confusion).
- **The gitsync default is ON wherever its gates pass** (tracked items.jsonl + upstream + no off-marker). Estate JSONL boards whose items.jsonl is tracked-with-upstream will start committing and pushing board writes on their next mutating verb. `~/.claude`'s carte board qualifies — its board writes will now auto-commit/push, which matches how sessions already treat that repo, but it is a behaviour change worth knowing when a "bon: edit carte-x" commit appears unbidden. `BON_SYNC=off` is the per-process brake; `.bon/sync` = `off` the per-board one.
- **mit-kg's GENERATED checkbox-less LEDGER.md means the flagship 8-person repo stays latest-wins by its own law** — the sweep keys on `- [ ]` markers the kg gardener doesn't emit. The reconciliation (gardener emits checkboxes, or kg opts out explicitly) is a kg-side decision for the mit fork session. Strategic, not urgent: un-ledgered dirs keep today's exact behaviour.
- Accepted residues, on record in the done notes: sidecar review is destructive-by-design after deletion (git history is the net); archive-vs-edit while-apart resurrects the item with no cue; a quoted filename in another ledger line's text counts as "mentioned"; baton rooms below depth 4 from the board root are invisible to draw-down from outside them (none exist today).
- His OTHER machines need a `spm1001/.claude` pull before their rites find the accent — until then those machines run the team spine (complete and silent, by design, but without his compass/tap).

### Opportunities
- The mit plugin's rite fork is now unblocked — the seam rule is satisfied. The fork session should read `docs/ACCENT.md` + this handoff's hedatu notes first, and owns: vendoring or not vendoring docs/ into the plugin (every "docs/…" citation currently dangles off-estate, a pre-existing class incl. CONTRACT.md), and the mit-kg ledger reconciliation above.
- bon-hofoju (which lane a close-tap successor line lands in) is now an ACCENT question — the tap text lives in his `close.personal`; one Sameer decision then two sentences in his own file, no publish needed.
- bon-libito (survey argv hardening) remains small and mechanical; bon-cedeze (missed-close net) is this board's biggest remaining rite item.
- The wevodu staleness warning (`_jsonl_staleness_warning`) is now redundant on sync-enabled boards — it fires just before the sync fetches for real. Harmless noise; fold into a later gitsync touch.

## For Claudes to come

**A falsifier written by the operator refutes what implementer-chosen axes pass — now measured five for five.** Every card tonight shipped green on its own tests before its essayeur attacked the operator's verbatim `--done` sentence; three of five were REFUTED on first contact, each on the clause the implementation had quietly weakened (offline edits "converge" by silently dropping one side; "zero dropped handoffs" except the rollout-week file; "surfaces that session's handoff" except on the one board shaped like real life). The pattern from the veleru handoff — an implementer picks axes inside the implementer's frame — held across two sessions, two authors and seven refutations, which upgrades it from "a pattern to test" toward a working law. The cheap discipline that made it pay: one essayeur per card, one axis (the operator's sentence), fresh context, never downshifted, and a post-repair round for every repair — a repair that never faced its own adversarial pass is unverified, and tonight one repair (guritu's sidecar) was itself refuted and needed a second.

**When a name is contested, key the format on ownership and let the pet name be prose.** Two live steers arrived mid-build: first "no metaphor names in core or format keys", then the settled uniform ids (`rite.personal`). Because the parser keyed on plain ids from the first steer onward, the second steer cost a rename, not a redesign — and the operator's pet names (compass, tap) survive as prose inside his own file where he can change them without touching anything shipped. The general form: when a human is still deciding what to call something, make the machine key boring and positional/ownership-based, and give the contested vocabulary a home that only its owner edits. A format key is a commitment; prose is a preference.
