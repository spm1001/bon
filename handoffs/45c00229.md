# Handoff — 2026-03-30

session_id: 45c00229-d15d-4c2d-974c-24eb6840b3cf
purpose: Landed WS3/4 — LLM auto-handoff, .bon/handoffs/ routing, /open deleted, 0.9.0

## Done
- Converted enrichment plan WS3-5 into bon actions with packed --what fields (the "how" hack)
- bon-mubava: Rewrote auto-handoff.sh — LLM-mediated via claude -p + ccconv in nohup background, mechanical fallback preserved, walks up to .bon/, resume deletes spurious auto-handoffs
- bon-pitide: Updated /close to write to .bon/handoffs/, close-context.sh walks up to find .bon/, /close skill instructions updated for new location and cross-project routing
- bon-karedo: Deleted skills/open/ entirely, cleaned /open refs from global CLAUDE.md and /close skill
- bon-dutuco: Version bumped to 0.9.0, pushed, deployed via batterie-update
- bon-rifima outcome closed (all child actions complete)
- open-context.sh updated to read from .bon/handoffs/ primary with ~/.claude/handoffs/ fallback
- session-start.sh: skip briefing on resume, delete auto-handoff on resume
- Filed feedback memories: "discussion is not capture", "bon wait needs IDs for auto-unblock"

## Gotchas
- The /close skill loaded in this session was still 0.8.3 (skill loaded before plugin update). Next session gets 0.9.0 with updated instructions
- close-context.sh in 0.9.0 cache computes HANDOFF_DIR via walk-up, but this session ran the 0.8.3 version which still outputs ~/.claude/handoffs/ — first real /close on 0.9.0 will exercise the new path
- auto-handoff.sh nohup bash -c has fragile quoting (bon-cahuwo filed)
- ccconv is NOT installed as a CLI — lives at ~/Repos/scratch/ccconv.py, auto-handoff.sh falls back to this path
- Plan file ~/.claude/plans/zippy-snacking-cocke.md still shows WS0-4 as not done — stale, bons are the truth
- hook-map.md in batterie-de-savoir repo needs updating (skipped, different repo)
- Mac ~/.claude/settings.json may still have old hooks block from before WS2 — needs checking on Mac

## Risks
- Auto-handoff LLM path has no fallback on claude -p failure — silently produces nothing
- First .bon/handoffs/ write hasn't been tested end-to-end via actual /close → restart cycle

## Next
- bon-gokofa: Plugin instruction shards via ~/.claude/rules/ symlinks (WS5 — last workstream)
- bon-cahuwo: Harden auto-handoff quoting and add silent-failure fallback
- bon-tufihu: Linked briefs for implementation choreography (parked, not urgent)
- Update hook-map.md in batterie-de-savoir repo
- Verify /close → .bon/handoffs/ → session-start round-trip works on fresh restart

## Commands
```bash
# Verify the new handoff location works end-to-end
ls .bon/handoffs/

# Check 0.9.0 is running
bon --version

# Verify ccconv is reachable for auto-handoff
python3 ~/Repos/scratch/ccconv.py --help 2>&1 | head -1
```

## Reflection
**Claude observed:** The session's rhythm was reactive until we filed bons mid-session — then the draw-down discipline kicked in and four actions landed cleanly in sequence. The "discussion is not capture" moment was a genuine turning point. The sed-emptied-CLAUDE.md incident was a reminder that ~/.claude/ files need extra care. The /close skill running from 0.8.3 cache while we deployed 0.9.0 is an inherent timing issue — skills load at invocation time from the cache that was current at session start.
**User noted:** "AAAAARGH" — frustrated that design discussions kept not being captured. Insisted bons should be self-sufficient without the plan file. Confirmed /open removal is correct (it's not listed in new session). Pragmatic about hacking "how" into "what" field until bon-tufihu lands.
