# Handoff — 2026-08-22

session_id: b7e73e1d-5520-4f2d-b502-003add7258b3
purpose: Map handoff routing end-to-end, measure load/placement aptness across the whole transcript corpus, then act on the verdicts — estate-wide migration to visible handoffs/, notes root-pile curation, and the guidance fixes the analysis implied
format: fond-v1

## For the next Claude

### Done
- Measured handoff routing quantitatively: 6,257 session transcripts (read side — what each session's start hook actually loaded, extracted echo-proof from `attachment` hook entries) × 1,092 handoff files (write side — where each sits vs its authoring session). Durable copy: `~/.claude/handoffs/handoff-routing-analysis-2026-08-22.md` (carte repo, 836d4de). Headline: current-era loads 236/237 own-room; the felt "load errors" were real but historical — June's notes-rooms gap and estate renames, amplified by the pre-July body-inlined briefings.
- Filed and largely landed bon-wipara (one placement convention, visible handoffs/): **bon-sanita DONE** — all 37 repos migrated by git mv and pushed (36 direct, mit-sales-lead-agent via PR #5, merged), plus the Mac's 8 clones bundle-pulled current with zero stranded files; **bon-vucumo DONE** — global fallback verdict KEEP (15 real uses on the Mac Apr–Jun; tube-only evidence would have deleted a working net).
- Curated the notes root pile: 33 room-specific handoffs moved to 13 rooms, all LEDGER links rewritten in place, curation note in the LEDGER header. Root is now honestly substrate-wide.
- Adopted the stray Cowork hello handoff into mit-commons (85b615a) — its mc-sejasu candidate had been minted there 9 Aug while the file stayed behind; that gap is now closed in guidance too.
- Shipped suite 1.70.3: /open's candidate-minting step gains the adoption verb (file travels with its board items); /bon:review's survey gains the root-pile sediment check. Shipped suite 1.70.5: deglacer's schema reference now documents `attachment` entries (hook output shape, the echo-proof jq recipe, the resume-fire caution).
- Filed crn-vitoti (cornichon board): the Mac's cornichon copy has no .git — can't sync, provenance unknown.
- Messaged modha-1a (the shared mit-kg pattern session) with the transferable conclusions; confirmed bon-d8 is inbound-deaf by billing route (Vertex sessions never bind an inbox socket — verified against env, socket table, registry and roster).

### Reflection
The analysis nearly manufactured three false findings, each caught by its own instrument check: Python's glob skips dot-directories (blinding a pass to every .bon/handoffs), estate renames masqueraded as ~190 cross-repo misplacements, and resume-fire hooks masked real briefings. Separately my own `2>/dev/null` ate a git bundle error and reported silent success — caught only because a confirmation string never printed. Sameer's soft doubts were well-calibrated twice: "I've seen load errors" (real, historical) and "no workaround, IIRC" (correct, now verified). The fence-audit method — per rung: current traffic × minting incident × current-era failures — adjudicated keep/retire cleanly and is worth reusing.

### Uncertain
- Pre-July read-side history is reconstructed against today's filesystem; 648 old loads are unresolvable (retired layouts). Sameer's memory of distant-repo loads is consistent with the body-inlined era but can't be fully adjudicated.

### Risks
- **bon-sedoze is NOT grep-and-delete.** bon ships publicly; external consumers' boards still read and write .bon/handoffs, so retiring the rung without a migration story breaks them. The converge-on-write precedent (storage.py's BOARD_README parasitic refresh) is named in its --how. Mac constraint is lifted (clones pulled); the consumer story is the real gate.
- The Mac's pca clone sits under the repo's old name (itv/pca vs mit-pca) — bundles pull fine and GitHub redirects, but it will confuse a sweep one day.
- Deliberately left at the notes root: the inter-repo briefs (bon ritual/web-service, MRTR — spent mail, and bon repo is public so adopting them needs a sensitivity pass) and a few mixed-subject files. Listed in the LEDGER header note; not an invitation to move them.

### Opportunities
- bon-sedoze is the next draw-down on this board: design the consumer-safe migration (close-context or `bon doctor --fix` converging piles on the back of normal use), soak, then retire the rung — its --how now carries the full sequencing, the CLAUDE.md doc-sweep addition, and the blast-radius warning. Dispatch line added to & Toolmaking.
- bon-rutoji is an open outcome with no actions (pre-existing) — confirm it's intentionally a sketch or break it down at the next review.

## For Claudes to come

Incident-shaped capture is structurally blind to drift: sediment produces no moment for intake to key on, so only clocked rites (reviews, surveys) see it — the notes root pile held 33 misplaced files for 11 weeks and surfaced only because Sameer asked a measurement-shaped question. Two norms follow, both now encoded on this board: a placement-convention change ships PAIRED with a sweep of the data the old convention left behind, and a reader's backward compatibility gets an expiry — tolerance of both shapes is compatibility from the code's side and camouflage from the estate's. Before retiring any "dead" path in a publicly-shipped tool, measure the blast radius beyond this estate: tonight's "zero-traffic" global fallback had 15 real uses on the Mac, and the "dead" legacy rung is live load-bearing for every external batterie consumer. Method worth reusing: the per-rung fence audit (current traffic × the incident that minted it × current-era failure count) turned "this is complicated" into three keeps, one retire-with-migration, and one keep-dormant — with receipts.
