# Handoff — 2026-07-21

session_id: 314efc2e-6e3f-4609-b33f-6bb36685b502
purpose: Adjudicated Bon-vs-GitHub-Issues (keep bon, three-ring doctrine); shipped the message-in-a-bottle (miheza) estate-wide + fixed standalone-blind orientation (cuvice); suite 1.17.0
format: fond-v1

## For the next Claude

### Done
- **Adjudicated Sameer's Bon-vs-Issues angst** — verdict banked in understanding.md ("Bon among the trackers"): keep bon; three rings (partnership / Hashimoto-locked team Issues / stranger inbox); the moat verified empirically (scratch repo `spm1001/issueform-probe`: REST API + gh both bypass Issue Forms' required fields — GitHub cannot refuse a briefless item at write, architecturally). Tripwires recorded, incl. GitHub's "agent issue assignment session management" roadmap item reaching GA.
- **bon-miheza DONE**: `bon init` now writes `.bon/README.md` (the stranger's projection of CONTRACT.md — read paths incl. the Dolt branch, the write gradient CLI→candidates→never-hand-edit) and prints a CLAUDE.md/AGENTS.md discovery stanza. 3 tests red-then-green. Considered-and-rejected in the brief: in-band JSONL header record (no magic-number convention exists; the `.bon/` directory is the envelope that travels).
- **Estate backfill swept 29 boards** (spm1001 × 26 + dotfiles + notes + bon itself): bottle everywhere, stanza into 14 CLAUDE.mds lacking a `.bon` mention, AGENTS.md created in 11 public repos. Pathspec'd commits only; all pushed (mise-en-space push race recovered by rebase). notes' bottle carried by notes-sync (commit `a25fb94b`, 23:16).
- **Ardoise proof (the miheza --done)**: blank-slate Claude, shimmed PATH (no bon, no uv), fixture board → discovered via stanza, read the bottle, correct ready computation, tried CLI first, then wrote a valid provenance-tagged `### Candidates` DONE handoff into `.bon/handoffs/` — items.jsonl checksum-identical across both runs. It quoted the bottle's invariants back as its reason not to hand-edit.
- **bon-cuvice DONE**: open-context.sh section 3 — under `set -euo pipefail` the `grep '^○'` pipeline exited 1 on outcome-less boards and killed the hook mid-section (the bare-header symptom was the script *dying*). Now outcomes and standalone filter separately, headers only emitted with content. 5 tests red-then-green (534 total passing); verified live against passe (6 standalone now visible).
- **Published suite 1.17.0** (minor: bottle = new capability; cuvice rode along). Assemble green, no quarantines, tube pulled current. scripts/ is vendored — the fix *required* the publish to avoid arming the 07:00 UTC daily-assemble ratchet.
- **Filed**: `bon-funice` (deferred sweep: 7 ITV repos + aboyeur), `bon-perare` (bottle refresh path + CLAUDE.md truing rides next publish), `gdn-catufu` (gueridon Dependabot critical — filed on gueridon's board).
- Housekeeping: released pujawo's finished tactical claim (all 4 steps were ✓; the *item* stays open on the organic leg, unchanged).

### Reflection
- The whole arc was side-quest-as-main-quest: an angst question → empirical adjudication → a filed design → shipped, proven, published, in one evening. The warm-context argument for doing miheza tonight was right — CONTRACT.md and cmd_init were already loaded from the design conversation.
- Verify-to-failure held: init tests red before implementation; cuvice's 4 red first; the ardoise run-1 "repo untouched" was correctly read as a rig artifact (`-p` deny-wall) rather than banked as pass or fail — run 2 at `--permission-mode=acceptEdits` produced the artifact on disk. Fixture checksum caught my own wrong-directory scare (relative-path `md5sum -c` from the harness-reset CWD).
- Sameer's asleep-time grant was scoped deliberately: ITV org repos and aboyeur (possible live sibling) excluded from the sweep; no interactive auth attempted for the probe-repo delete.

### Uncertain
- Whether waiting standalone actions (⏳-suffixed but still `○`) should also surface in orientation — they do now (glyph matches). Deliberate, but nobody's confirmed the taste call.

### Risks
- **`spm1001/issueform-probe` still exists** (private, 2 junk issues) — deleting needs `gh auth refresh -h github.com -s delete_repo` (interactive; Sameer: `! gh auth refresh -h github.com -s delete_repo` then any Claude can `gh repo delete spm1001/issueform-probe --yes`), or two clicks in the UI.
- **Do not edit bon's CLAUDE.md (or any vendored file) without riding a publish** — bon-perare carries the pending truing (test count, init's new outputs). A lone pushed edit gets bon quarantined at the next daily assemble.
- Other machines' plugin caches lag 1.17.0 until their next `/batterie:update`; the orientation fix only activates after a session restart even on tube.
- The 14 stanza-appends sit at EOF of their CLAUDE.mds — fine, but reposition opportunistically when those files are next touched.

### Opportunities
- **bon-funice**: the deferred sweep (needs Sameer's yes on ITV push-vs-PR + an aboyeur sibling check).
- **bon-perare**: bottle refresh path decision + CLAUDE.md truing on the next publish from this repo.
- **gdn-catufu** (gueridon board): the Dependabot critical on an always-on tailnet service — worth a prompt look.
- **pujawo/camoru organic leg** unchanged: a real Cowork session doing genuine notes-board work → real candidates → real mint.
- gueridon deploy of dd38234 still HELD (last session's item) — unchanged tonight.

## For Claudes to come

The tracker adjudication and its tripwires are **already written to understanding.md** ("Bon among the trackers", plus the rejected in-band-header note in bon-miheza's brief) — don't re-integrate. Two things worth synthesis beyond that:

1. **The bottle exists and is docket-side.** Every board now carries `.bon/README.md` — the stranger's projection of CONTRACT.md's candidate mode, written by `bon init`, static by design. Its enforcement is prose, not code: it works by making the conforming path cheapest (the ardoise stranger followed the CLI→candidates gradient unprompted and refused the hand-edit *citing the bottle's own invariants*). Refresh path for existing boards is an open gap (bon-perare). When editing the bottle text (`BOARD_README` in cli.py), remember 29+ static copies exist.

2. **A session-ritual hook must not die on data shape — and under `set -euo pipefail`, a bare `grep` in a pipeline IS a death sentence when it matches nothing.** cuvice's bare-header symptom was the hook being killed mid-section by grep's exit 1, not a filter quietly returning empty. The pattern that survives: capture with `VAR=$(... | grep ... || true)`, emit headers only when the variable is non-empty. Same family as the xhigh sidecar lesson already in understanding.md (validate cross-tool input), but the killer here was the hook's *own* pipeline discipline.
