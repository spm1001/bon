# Handoff — 2026-03-30

session_id: cc55bb28-5df2-497f-9700-b1e931725ebc
purpose: Designed 6-workstream plan for enriching bon's session lifecycle hooks (pobumi + kuriwe)

## Done
- Fixed /close skill find pattern — added `-not -path "*/skills/*"` to prevent matching audit skill's scripts/ directory (committed, pushed)
- Synthesized contribution into understanding.md (new "skills layer" section)
- Resolved rebase conflict with remote's softer /close Decide gate (took remote version)
- Researched Claude Code hook infrastructure — 28 events, we use 3 (full inventory in batterie-de-savoir/docs/hook-map.md)
- Created hook-map.md and hook-map.svg in batterie-de-savoir/docs/ (also PNG in ~/Taildrive/)
- Discovered garde-manger not installed on Mac (enabled in settings but missing from installed_plugins.json)
- Confirmed ~/.claude/rules/*.md auto-loads at user level — plugins can own instruction shards via symlinks
- Filed bds-tujede in batterie-de-savoir: batterie meta-plugin should own session infrastructure
- Filed bon-rifima: handoff location migration to .bon/handoffs/
- Filed bon-rabaru: prototype redesigned briefing before implementing
- Activated work-pobumi tactical steps (in work repo, now stale — need bon work --clear and re-plan)

## Gotchas
- work-pobumi tactical steps are stale — the plan went far beyond "audit open-context.sh". Next Claude should `bon work --clear` on it and re-plan from the brief or just work from the plan file
- The plan file is at ~/.claude/plans/zippy-snacking-cocke.md — THIS IS THE PRIMARY ARTIFACT. Read it before starting execution.
- batterie-de-savoir/docs/hook-map.md and hook-map.svg were written but NOT committed (dirty in that repo)
- The test shard ~/.claude/rules/test-shard.md should be cleaned up after confirming the rules/ pattern works
- Remote had a softer /close Decide gate rewrite — we took that version. The stricter "bon is DEFAULT" language from the earlier local commit was dropped.

## Risks
- The plan is ambitious — 6 workstreams touching hooks, scripts, skills, CLAUDE.md, plugin.json across multiple repos. Needs disciplined sequencing per the execution order in the plan.
- ~/.claude/rules/ symlink pattern untested with plugin cache paths specifically — confirmed auto-load of direct files, not symlinks through versioned cache directories
- Rich auto-handoff via `claude -p` on SessionEnd depends on generous timeout setting and the ccconv transcript parser (not yet verified)
- Deleting /open is irreversible — make sure the hook truly replaces it before removing

## Next
- Read the plan: ~/.claude/plans/zippy-snacking-cocke.md
- Execute Workstream 0 first (extract updater from bon) — prerequisite
- Then Workstream 1 (redesign open-context.sh) — start with bon-rabaru (prototype)
- bon-rifima (handoff migration) spans Workstreams 3 and 4c

## Commands
```bash
# Read the plan
cat ~/.claude/plans/zippy-snacking-cocke.md

# See bon state
cd ~/Repos/batterie/bon && bon list

# Check work-pobumi (stale tactical)
cd ~/Repos/work && bon show work-pobumi

# Commit hook-map in batterie-de-savoir
cd ~/Repos/batterie/batterie-de-savoir && git status
```

## Reflection
**Claude observed:** The session was pure design — no code written beyond a find-pattern fix, but the plan is comprehensive. The key discoveries were: (1) ~/.claude/rules/ auto-loads user-level instruction shards, enabling plugins to own their CLAUDE.md content; (2) Stop hooks fire every turn making them poor candidates for session-end detection; (3) the existing auto-handoff is thin because it doesn't use the transcript, but garde-manger's pattern (LLM-mediated via claude -p) shows how to make it rich; (4) handoffs keyed by CWD break the user's cross-project workflow — keying by .bon/ project with a continue_in field solves it. The user's instinct to walk through the plan chunk by chunk was right — several assumptions were corrected (Stop hook abandoned, contributions restored, instructional footer scoped down, understanding.md + handoff both included in full).
**User noted:** Side quests (hook map SVG, machine parity audit) were productive — found garde-manger missing on Mac. The updater scripts are "peculiar" — infrastructure that landed in bon by accident. The redesigned briefing (~18 lines vs 75) looks right but needs prototyping. "If it ain't broke, break it anyway."
