Handoff written to `.bon/handoffs/c184068d.md`. Here's what was captured:

---

# Handoff — 2026-03-31 (auto)

session_id: c184068d-515e-4fbe-8c94-65bfdab4e856
purpose: Update batterie plugins to latest marketplace versions and fix CLI version drift

## Done
- Refreshed batterie marketplace and updated all plugins
- Fixed CLI drift: `bon` 0.9.0 → 0.10.0 (includes `[dolt]` extras), `todoist` 0.4.3 → 0.4.5
- Confirmed garde-manger, batterie, mise, trousse already current (no drift)
- Used `--force` flag on npm cache clean to get past lock during bon CLI update
- Session also landed: merged `/open` + `/tracker` into single `/bon` skill with progress indicators (1b5c223), version bump to 0.10.0, contribution notes (84e880b, b582d9c)

## Next
- Restart Claude Code (`/exit` then `claude`) to activate updated SessionStart hooks — `/reload-plugins` is insufficient
- Investigate bon-cahuwo: auto-handoff nohup quoting is fragile and failures are silent
- Investigate bon-bohaki: close-context.sh still outputs legacy handoff path
- Verify bon-fobewu: skills/bon/ directory rename resolves correctly in plugin system
- Consider bon-tufihu: outcomes linked brief for implementation choreography
- Consider bon-gokofa: plugin instruction shards via ~/.claude/rules/ symlinks

## Gotchas
- Auto-generated handoff — no reflective close was performed
- todoist-gtd CLI drift (0.4.3 → 0.4.5) was pre-existing before this session, not introduced here
- bon CLI update required `npm cache clean --force` to bypass lock — worth noting if update process is ever scripted
