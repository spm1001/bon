# Handoff — 2026-03-31

session_id: 89a11168-2e3d-4ec3-b255-7a2f7d4a7d7f
purpose: Handoff resolution eliminates legacy ~/.claude/handoffs/ entirely

## Done
- Diagnosed why session d84f0257 (deglacer, launched from ~/Repos) wrote handoff to legacy ~/.claude/handoffs/ — walk-up from container dir found no .bon/
- Added scan-down to close-context.sh and auto-handoff.sh: finds child repo with most recent git commit when walk-up fails
- Added global fallback ~/.bon/handoffs/ — legacy path never used for writes
- Removed legacy read path from open-context.sh (was checking ~/.claude/handoffs/ + cross-machine Mac↔Linux prefix swap)
- Removed legacy duplicate-check from auto-handoff.sh
- Migrated all handoffs from ~/.claude/handoffs/ on hezza: 96 to project .bon/handoffs/ (15 repos), 102 to ~/.bon/handoffs/ (global), 36 aboyeur test artifacts deleted
- Deleted ~/.claude/handoffs/ directory entirely
- Filed bon-fajefa: Mac still needs same migration
- 431 tests pass

## Gotchas
- Scan-down uses `find -maxdepth 4` from CWD — fast for ~/Repos (~20 repos) but could be slow from $HOME. Filtered to git repos only (skips pytest temp dirs).
- The "most recent commit" heuristic picks the wrong repo if the session made no commits (pure reading). Falls through to global ~/.bon/handoffs/ which is acceptable but not project-specific.
- Mac ~/.claude/handoffs/ is untouched — old handoffs there are invisible to the updated open-context.sh until migrated (bon-fajefa).
- `.bon/handoffs/` directories aren't .gitignored — migrated handoffs appear as untracked files in project repos. Decision needed on whether to git-track them.

## Risks
- No automated tests for the scan-down bash logic in close-context.sh / auto-handoff.sh — verified manually only.

## Next
- bon-fajefa: Migrate Mac legacy handoffs (same operation, different machine)
- Decide .gitignore policy for .bon/handoffs/ across repos
- bon-watoda, bon-duvuwi, bon-jihoko remain from last session's suggestions

## Reflection
**Claude observed:** The diagnosis was straightforward (walk-up from container dir finds nothing), but the fix went through three signal iterations — items.jsonl mtime picked the wrong repo (most recently modified ≠ most recently worked in), commit count favoured repos with many small commits, and most recent commit timestamp correctly identified the session's repo. The migration was mechanical but revealed the encoding is lossy (hyphens in names conflate with path separators), which we sidestepped by matching from real paths rather than decoding.
**User noted:** Pushed to eliminate legacy entirely rather than keep backwards compat — "we should never use ~/.claude/handoffs/ surely?" and "why can't we migrate the old ones?" Both correct instincts that simplified the final design.
