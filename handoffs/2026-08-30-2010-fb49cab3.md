# Handoff — 2026-08-30

session_id: fb49cab3-94db-4e47-9d33-1cb4607c695f
purpose: The rites speak the lane era — /review populates the lanes through a socket, /open's compass reads the sectioned queue (bon-veleru, bon-vatoge)
format: fond-v1

## For the next Claude

### Done
- **bon-veleru closed.** skills/review/SKILL.md Phase 1 and skills/review/references/pyramid-format.md rewritten: lane population is the ceremony's product, delivered through a variation-point socket (`review.populate-queue`) riding the five-phase spine — a team with no dispatch queue runs the rite complete, skip named in one plain line. The three ceremony-proven mechanics carried from the 23/29 Aug runs: pre-annotation from one survey pass, bucket-is-verdict triage, and the two-writer write-window protocol with the race named in both files. Alignment block re-specced (the lanes ARE the join, both directions); docs/CONTRACT.md's falsifier venue re-scoped to the Up Next threshold.
- **bon-vatoge closed.** skills/open/SKILL.md §4: the compass reads sections FIRST, renders this-repo matches lane-tagged plus lane counts within the three-arrow-line budget, gates on exit status never empty output, and discriminates the book on the not-found branch (loud on Sameer's book, silent on a foreign one). The `section_id==null` P1-DO read is dropped with a dated note — measured 2026-08-30 it matched nothing twice over (no priority-4 tasks left in Projects - Toolmaking; its outcomes are drifting into sections, invisible to task-grain queries).
- **Shipped as suite 1.82.0**: content commit a4393db (`git show a4393db` is the complete diff of everything this session changed), stamp 4bd8e56; assemble run 33332410531 read green independently (`gh run list --repo spm1001/batterie` — the run lives there, not in this repo). This machine pulled; bon CLI reads 1.82.0.
- **Sameer's falsifiers filed verbatim** on both cards mid-work (relayed by the dispatcher from his answered falsifier sheet — the same channel that set the Common Core socket steer): veleru "I get muddled about what the review is doing"; vatoge "The wrong bits bleed into the team /open vs my individual one". Both essayeurs re-ran on those axes; both REFUTED the then-current text. The repairs shipped as direct transcriptions of the verifiers' own quoted findings — **no post-repair essayeur round was run**, so the repaired text has passed no recorded adversarial pass; the live run is its second pass.
- **Filed**: bon-libito (audit_survey.py `--repos` edges — empty filter silently widens to the estate, substring match over-includes, no jobs-group flag) and bon-hofoju (the /close tap's propose-and-add sets no section, so a clean-case successor line lands silently in backlog — lane default is Sameer's call).

### Reflection
- **Six verifier rounds, and the two falsifier-axis rounds found different faults than the four implementer-chosen axes.** My axes (correctness, socket detachment, render truth) were all green or repaired by mid-evening; his two axes then each REFUTED the same text — muddle found the missing Phase 5 queue read-back and window closes that counted moves instead of itemising them; bleed found that my own repair (the one-line "not shown" render) was itself the nag, with a live precedent (Stef hit "Project '& Toolmaking' not found" on 2026-08-11 — recorded in bon-hedatu's `--why`). An implementer picking essayeur axes picks inside the implementer's frame.
- **The sharpest single catch was a null that happened to agree with the truth**: the compass spec said sort by `child_order`, accomplis emits that value as `order`, and a jq on the wrong name null-sorts into API return order — which today matches drag order. The cold reader caught it only by hand-verifying the render against raw output. Field names are per-surface; loop.md's vocabulary is the Sync API's, not accomplis's.
- **Write-then-verify ordering worked as designed**: `bon step --no-complete` held both cards open through two verifier rounds each, and the falsifiers arriving mid-work slotted straight into already-running essayeur contexts by SendMessage rather than fresh spawns.

### Uncertain
- **The watch cell**: an MIT teammate whose own Todoist legitimately carries a "& Toolmaking" project would sail both compass gates and get their queue rendered under Sameer's lane semantics, silently. No live instance; the essayeur marked it watch, not refuted. bon-hedatu's accent extraction is the cure — the whoami discriminator added tonight only fires on the not-found branch.
- Whether the socket name `review.populate-queue` matches the Common Core page's own naming. The phrase "scope dial" nulls estate-wide, but Common Core itself traces: the 29 Aug pyramid snapshot's mechanics footer records "a live sync with the Common Core design session", 2026-08-25 session captures in `~/notes/raw/claude/code/` mention it, the design session was live in tmux window claude:1 tonight, and the page itself is most likely a Drive doc (mise) or Sameer directly. The name used here follows bon-hedatu's boring-machine-name law and the dispatcher's steer; hedatu's implementation session should true it up against the page via one of those routes.

### Risks
- **Nothing behavioural in these edits was verifiable in this session** — skill text and CLAUDE.md-class files are session-cached. In-session verification was four fresh-context Fable cold-reads/essayeurs against the real queue (read-only) plus two falsifier-axis rounds; **live verification belongs to the next real /open and the next real /review ceremony**.
- **Before judging any live misbehaviour, confirm which plugin version the session actually loaded.** At close, `~/.claude/plugins/installed_plugins.json` held a project-scope pin for projectPath /home/modha at **1.81.0** (stamped 19:43Z, before the 20:01Z publish) beside user scope at 1.82.0 — so a $HOME-launched session may load PRE-repair skill text, and "the new compass misbehaved" could be the old text running. Only once the loaded version reads 1.82.0 does misbehaviour indict this ship — and then suspect the text before the tools: the CLI surface underneath was probed and matches.
- The compass now costs one extra API call (`accomplis whoami`) on the not-found branch only. On the happy path the cost is unchanged (two calls).
- pyramid-format.md still cites `~/scratch/estate-pyramid-*` and `~/notes/...` exemplar paths that exist only on Sameer's estate — known, flagged by the essayeur, and deliberately left: that residue is bon-hedatu's extraction, not this card's.

### Opportunities
- bon-hedatu (the four-rites accent split) now has a durable input: `docs/accent-audit-2026-08-30.md` compiles tonight's essayeur findings — every Sameer-tissue line in the review and open spines, plus the open watch cell (a teammate whose own book carries a "& Toolmaking" project sails both compass gates). The essayeur reports themselves lived only in subagent context; the sidecar is what survives.
- bon-libito is small and mechanical (survey script argv hardening); bon-hofoju needs one Sameer decision (which lane a successor line lands in) then two sentences of tap text.
- bon-gimelu (gitignored-handoffs warning gap, from this afternoon's sedoze worker) is still open and unrelated to tonight's work.

## For Claudes to come

**A socket is cut in two places, and the second is where it fails.** Declaring the variation point — "step 4 is pluggable, skip named without it" — took one paragraph and passed every read. What failed the cold walk was the tissue around it: the assembly sentence that still listed the queue unconditionally, the refill trigger with no lane-existence guard, the alignment step keyed on "unreachable" when a teammate's book is "absent", the frontmatter qualifier attached to one clause of two. When you socket a step out of a rite, grep the whole rite for every sentence that assumes the step ran — the declaration is the easy half.

**The operator's falsifier attacks the frame, not the work.** Four implementer-chosen verification axes (correctness of render, socket detachment, truth under both section states, executable gates) all went green before Sameer's two verbatim `--badly` lines each refuted the same text within the hour: "I get muddled about what the review is doing" found that the ceremony's last queue write trailed off hours before the rite ended with no read-back; "the wrong bits bleed into the team /open" found that the repair one of my own verifiers had proposed — a visible one-line "not shown" render — was itself his furniture nagging a stranger's book forever (live precedent: a teammate hit that exact error line on 2026-08-11). The likely mechanism — observed twice in one evening on one author's falsifiers, so a pattern to test rather than a law: an implementer picks axes inside the implementer's frame, and what the text is FOR is exactly what the implementer can't see past. The rule that stands regardless of the mechanism: when a human-authored falsifier exists, run it as its own verifier round even after your own axes pass; the cheap form is one SendMessage to an essayeur that already holds the context.
