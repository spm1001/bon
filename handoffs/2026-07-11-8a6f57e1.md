# Handoff — 2026-07-11

session_id: 8a6f57e1-a685-4473-a038-53113de8aa6d
purpose: Estate-wide bon cull driven from Sameer's "Bon estate" Google Doc, evolving the bon↔Todoist boundary
format: fond-v1

## For the next Claude

### Done
- Cull driven from the "Bon estate — open desired outcomes" Google Doc (`1TlAJ467Adc8gAiEZPT8CCLhgGDto5SwfQ8peqjhtyHU`), Sameer marking via **checkboxes** (done+salvage) + **margin comments** (nuance/steer). Read ticks via Drive `text/markdown` export, comments via mise re-fetch.
- **§1 engine room** (18 ticked): 16 closed with salvage, 2 re-homed — `iw-vovipa`→glaneur `glan-roguza` (chats feeder), `bds-joluto`→mit-commons `mc-zotoze` (team-platform blueprint). Kept-live standalones: `iw-hehihu` (offsite backup — must precede hezza cancel), `bds-vegowo`, `passe-ropuze`. New: `iw-dovali` (tube-secrets), `mise-newosi` (doc-triage capability).
- **§3 ITV**: `wiki-kuwewu` closed (roadmap salvaged → NEW `mit-wiki/.bon/understanding.md`); `opx-*` (5) closed (thinking already in op-exploration's self-contained docs). plongeur + `sf-*` untouched.
- **§4 notes/work-life** (26 outcomes + 22 stray actions): fully swept. Work-life → Todoist (Sameer ports by hand); already-done archived; substrate/safety kept as bon actions. `namuti` DONE — `build-phase1-cost-model.py` reconciled to £5k/advertiser flat / £118,500 total (verified; NOT re-run against the live SoW sheet — that outward push is Sameer's).
- **§5**: `cin-hiboza` closed+archived; `cio-*` (5) held for Sameer's pass; `day`/`tmig` were already orphan-closed (doc was stale).
- **cosazi** (clean-room C2 fail): audited live via the clean-room skill — real config gap, Sophus3 has an ACTIVE sub linking 2 policy-less Ford views, BUT the data-content check de-escalates it (both are metadata / aggregated counts, no individual identifier; the `ip_address` view `itvx_exposure_test` IS policy-protected). Draft email to Alexander Green cc Rupert **staged, unsent**.
- Google Doc **regenerated to 67 open outcomes** (curation preserved, ticks reset, real checkboxes, new items folded).
- Salvage committed+pushed: bon/batterie understanding.md, macos-notes.md, mit-wiki understanding.md. JSONL closes committed (mit-wiki, op-exploration, claude-intents, glaneur, mit-commons). notes board syncs via notes-sync.
- Merged `iw-hamuda` dup → `bon-mevifu`.

### Reflection
- Present→confirm→execute per section worked. Sameer's ticks (which) + comments (why) are a genuine steering channel once the read mechanism was sorted.
- Search-before-mint slip: filed `iw-hamuda` without checking `bon-mevifu` existed. Merged.

### Risks
- The Doc's checkbox **write** works, but **reading** ticks still needs `mise-newosi` — the Docs API doesn't expose checked-state; ticks are read via the Drive `text/markdown` export, not a plain mise fetch. Don't assume `mise fetch` surfaces ticks yet.
- `cosazi` is live until Alex acts — the email is drafted, unsent (Sameer sends). The DCR-safety trio `cosazi`/`himefo`/`hadare` are kept OPEN on the notes board deliberately (safety, don't close-and-hope).
- Notes board still holds 12 substrate/safety actions that are re-home candidates (`bunuju`→bon, `celugo`→itv-slides-formatter, `pehusa`+`jilupa`→with `pulewu`, `gilomi`→glaneur) — a future (c) tidy, not done.

### Opportunities (the baton)
- **NEXT SESSION, agreed with Sameer (2026-07-11): plongeur + cornichon.** These two are the planned focus of the next sitting — start here.
  - **cornichon** — the big product bet skipped in this cull (§2, the `bon-*` outcomes on the **cornichon JSONL board**: Session view/grammar, antigravity-SDK, open/save, notarized install, persona calibration — ~15 outcomes, many with deep action trees). Not touched this session; ripe for its own review.
- **plongeur** (mit-plongeur) — Sameer wants a DISCUSSION: "kill the ADK nonsense, focus on a few simple ingest/output features." 52 open `plg` items under 15 ADK-heavy outcomes. Prep by mapping ADK-scaffolding-to-kill vs ingest/output-core-to-keep; do NOT mass-close blind.
- **cio** — Sameer's one-more-pass before ditching claude-in-office (5 outcomes held).
- **kosawe** (notes) — Claude-doable: repoint `build_word_template.py` to fetch its 2 logos from Drive (broken since the ~/work fold). Offered, not done.
- **bon-mevifu** — migrate the ~17 live JSONL boards to Dolt where appropriate.
- Sameer's hand: port the Todoist cards from the Doc; send the cosazi email.

## For Claudes to come

- **The bon↔Todoist boundary, now operative.** Work-life (Sameer's actual job — Lantern, Adalyser, Sky transition, GCP estate) leaves bon → **Todoist** (his trusted GTD); **bon is for the estate/tooling**; substrate (field-reports, tool fixes) stays as bon. The eventual mechanism is `bon-wivuti` (a lean Todoist projection of a shared work-atom); the interim is Sameer hand-porting from a Claude-produced digest — deliberately, because an auto-sync would pollute his trusted book with Claude's working-memory churn. (Also banked in bon `understanding.md`.)
- **The doc-triage channel.** A Google Doc with checkboxes + margin comments is a viable human→Claude steering surface. Ticks: the Docs API does NOT expose checked-state, but the Drive `text/markdown` export preserves `- [x] ~~struck~~`, and mise's `overwrite` renders `- [ ]` as real checkboxes. Comments: mise's re-fetch deposits `comments.md`. `mise-newosi` tracks making a single plain fetch surface both.
- **Updating a curated doc: transform-in-place, don't raw-regen.** A live estate dump re-clutters with items the curation deliberately pruned (a raw regen this session inflated ITV 27→31 by pulling in vohifo/trends-api boards). Preserve the human's curation: drop closed, reset marks, fold in the new.
