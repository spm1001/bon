# Handoff — 2026-06-20

session_id: 6fb03d4d-461c-41d2-98b9-2225141efcfd
purpose: Propagate bon's ensure-script diagnosability/--no-cache fix to the sibling CLI hooks (bon-mavemi) — and fix a family-wide latent JSON bug found while verifying
format: fond-v1

## For the next Claude

### Done
- **bon-mavemi** (closed): propagated the babuse diagnosability + `--no-cache` fix to **passe** (0.6.3, `020142f`) and **todoist** (0.4.7, `c0d3158`). Premise correction — the brief said "4 sibling repos," but only **2** are CLI-installers; tafelmusik/mise are MCP-server ensurers (`uv sync`, no `uv tool install`), so `--no-cache` is a category mismatch. passe/todoist already had the git+https install line (the "CLI won't install" 404 was bon-only, fixed this morning by `bdd4bce`).
- **Family-wide latent bug, found + fixed while verifying mavemi's `--done`** ("forced-failure render produces valid JSON"): passe/todoist/mise all rendered hook output via raw `cat <<EOF`, and the failure message's quoted `$INSTALL_SRC` / `$PLUGIN_ROOT` closed the JSON string early → invalid JSON on the failure branch. Switched all three to `python3 json.dumps` (bon's pattern from the start). `bash -n` and every success-path test were blind to it; a forced-failure render piped to `python3 -m json.tool` is the only check that catches it.
- **bon-dotupu** (filed, then narrowed): **mise's half shipped** (0.7.11, `22fc7cc` — json.dumps render + `uv sync` stderr→`~/.cache/mise/ensure.log`). Remaining scope = **tafelmusik only**.
- **Full publish dance**: assembled (run `27874502787`, success), marketplace + plugin update, CLI reinstall, coherence-verified (every installed CLI == highest cache version: bon 0.26.5, passe 0.6.3, todoist 0.4.7, mise 0.7.11).
- **Session-open synthesis** into understanding.md: jejuge read-normalization lesson, corrected `OPTIONAL_BRIEF_FIELDS` location (it's in `display.py:10`, not storage.py), `--no-cache` publish note. **Plus** this session's ensure-family / json.dumps insight, integrated directly (see Reflection).
- Cleared a stale `uv run bon` 0.23.1 build in the dev tree → 0.26.5 (`uv sync --reinstall-package bon --extra dolt --no-cache`; pymysql restored).

### Reflection
- The throughline: **the brief was wrong in two directions** (4→2 repos; a 2-part fix that grew a 3rd part), and each correction came from *running* the verification, not reasoning about it. The forced-failure harness — not `bash -n` — found the latent JSON bug. Ledger #13 (a green check must exercise the real failure at the real layer) and the field-report-premise lesson, both confirmed.
- Folded mise into the *same* assemble rather than running the whole-marketplace CI twice (Sameer's phrasing was "dance then mise"; agreed the one-pass efficiency). One assemble vendors every repo from GitHub HEAD, so batching is free; the version bump keeps the ratchet satisfied.
- The mtime-glob gotcha (understanding.md "Script resolution") bit my *own* coherence-check code — `ls -td` picked the wrong dir because new version dirs have older mtimes; `sort -V` fixed it.
- **I integrated this session's durable insight into understanding.md directly** (the ensure-family two-shapes + json.dumps-render note), so this handoff deliberately has **no "For Claudes to come"** — there's nothing left to compost, and a copy here would make the next `/open` re-synthesize a duplicate.

### Uncertain
- `pymysql.__version__` reports `2.2.8` in the dev venv while the installed package is `1.1.3` — cosmetic, import succeeds; not chased at close.

### Risks
- **tafelmusik untouched** (bon-dotupu): a failed tafelmusik server start is still undiagnosable and emits inconsistent hook output (JSON on success, plain text on the systemd fallback).
- The render now hard-depends on `python3` at session start for passe/todoist/mise — was already true for the version-drift check, so no new dependency, but worth knowing.
- **The fixes are live in the plugin cache but the active session runs the OLD hooks until a full restart.**
- Sibling **Dependabot** alerts surfaced on push (passe 7, todoist 1, mise 12) — out of scope; `/github-cleanup` owns them.

### Opportunities
- **bon-dotupu** — finish tafelmusik (the judgment call: make the systemd fallback emit JSON like the success branches, or deliberately keep it plain text). Same forced-failure harness verifies.
- **bon-nagigu** — a `bon publish` / `batterie publish-shard` verb would automate the dance I ran by hand this session; the recurring "family one version behind in cache" state argues it's worth building.
- **bon-pulaga** — the rest of the CLAUDE.md tidy (dependency rules; stale counts).
- The **legible-substrate** cluster (camoru / walile / zopopu) still wants an outcome minted in Sameer's framing word — carried across several sessions now; his word to set.
