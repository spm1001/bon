# Handoff — 2026-08-16

session_id: 667955d1-ebe6-4c8c-b520-7cce4c1959b2
purpose: kohure closed and the defect pile cleared — three releases (1.58–1.60), the area field, doctor's order repair, and the caller-mistake ladder made doctrine
format: fond-v1

## For the next Claude

### Done

- **bon-kohure closed — "The docket speaks full GTD" is complete**, all ten children done. The two finishers: **bon-wivuti** adjudicated the 2026-06-17 work-atom sketch's schema deltas (`context`, `tier`, `provenance`, tier-gated briefs all REJECTED with reasons in understanding.md; every rejected field served the dead shared-store premise, every accepted one serves Claude-continuity — the sketch doc carries a supersession note). **bon-razonu** shipped the `area` field (1.58.0): someday-pattern storage (Dolt VARCHAR(100), idempotent migration proven on a scratch server both fresh-CREATE and ALTER paths), `--area` on new/edit, `bon list --group-by area` / `--area X`, grouping keyed on top-level entities (subtrees travel with their outcome's area).
- **The defect pile cleared, five standalones → one** (1.59.0): **tagoje** — `bon doctor --fix` resequences duplicate-order sibling groups 1..N, both backends, gated on a parseable file; the mover's re-mint behaviour is pinned by a test as documentation. **vomuzi** — all 18 lookup-miss sites name the board searched and its cwd, with a "wrong directory?" callout on foreign-prefix ids. **numise** — the frozen-turn-counter/restart-banner chorus was `session_id` arriving empty on hook stdin, dropping the state key to `$$` per turn; fixed with a `CLAUDE_CODE_SESSION_ID` fallback (confirmed present in a plain interactive `cli`-entrypoint session — which also cleared casovo's old caveat). **vinije** — the close skill's corpus-edit rule now splits: new rows defer to intake, mechanical corrections go straight in.
- **bon-siciri adjudicated with Sameer and shipped (1.60.0), upgraded by triangulation.** His "Ronacher" pointer surfaced two same-day conversations: the new `~/notes/practices/tool-ergonomics/` room (theory) and its first diagnostic (300,637 estate tool calls mined — bon is the most-called estate CLI and among the healthiest at 3.7%). The result is the **caller-mistake ladder**, now doctrine in `docs/CONTRACT.md`: never absorb silently (forgiving harnesses train slop — Ronacher's mechanism), refuse loudly naming the right move (cheap: 76% of errors clear on the next call), coach, or bend to a strong unambiguous prior *with the repair reported in the tool result*. Verdicts: verb-noun grammar declined with a tripwire (revisit when erg-vikoke's invocation logging makes it countable); `bon edit --append-how` shipped (atomic annotation, retiring the read-modify-write recipe — open SKILL.md rewritten around it); `bon new` announces its species ("Created outcome:") since an errorless wrong-species mint is invisible to transcript mining; `convert` gained `-q` and the `none` spelling edit itself teaches.
- **Bycatch, two real bugs found by probes doing other jobs:** dolt.py carried an unstripped twin of `load_prefix` — a hand-written `.bon/prefix` with a trailing newline made every Dolt read silently empty while writes landed; fixed by deleting the twin (storage.load_prefix is now the single reader). And the session dashboard was entirely mute in non-repo cwds (pipefail vs `git status` 128) — silent since forever, fails-open hid it.
- 766 tests green; suite went 1.57.1 → 1.58.0 → 1.59.0 → 1.60.0, all assembles green, tube current.

### Reflection

Sameer's light touches steered better than the board did, twice: "have a think about the overall state" produced the kohure-first plan, and his one-line reframe ("are we choosing how the command behaves when the calling agent makes a mistake?") plus one search term turned four ad-hoc verdicts into written doctrine. The pattern from his CLAUDE.md — a soft question carrying hard cargo — held all evening. One self-catch worth owning: my sweep for `"Created: "` parsers ran through `head` and missed two; the suite went red exactly as designed, but the instrument's-limit trap fired on me an hour after I'd quoted it.

### Uncertain

- The compass's repo-matching positive branch is STILL unexercised — third and fourth live renders both truthfully said "nothing points here" (queue 31→34 over the evening, none at bon). Proof still wants an /open in gueridon, cornichon, or carnet.
- Whether CC's missing `session_id` on UserPromptSubmit stdin is a version regression or a route property — the env-var fallback makes it moot for us, but the upstream question is unanswered.

### Risks

- **A parallel lane worked this tree tonight and landed between this session's last publish and its close commit**: `d98d6b3` — v4 handoff filenames (`YYYY-MM-DD-HHMM-id8`, reader ties broken by filename time, notes-sovike). Two consequences: this handoff's own filename is the *old* v3 shape (written by the 1.60.0 cache script, correct at the time); and that commit is **unpublished** — the next `/batterie:publish` from this repo ships it, so don't be surprised when a "docs-only" release carries script changes too.
- **Three releases of session-cached text land at your session, not the one that shipped them**: open SKILL.md (area line, --append-how recipe), close SKILL.md (corpus-edit split), CLAUDE.md (area, gotchas), plus the dashboard hook fix. Don't read their absence in a stale session as breakage — and expect the restart-banner chorus to be GONE from your headers; if it persists, the numise fix needs re-examination.
- **`area` decays under old-client Dolt writes** — same accepted trade as someday, fingerprint: `updated_by` fresh while `area` reads NULL. Converges as machines update.

### Opportunities

- The Sunday reconciliation ceremony is due 17 Aug, fresh session — it reads a board with kohure complete and one deliberate straggler.
- bon-jezahi (gen-rooms.py emits each room's paired repo) is the one substantial item left — best run fresh, ideally beside ~/notes work, since its done-criterion regenerates notes' rooms.md against Sameer's adjudicated pairings.
- The erg board (~/notes/practices/tool-ergonomics) will bring bon obligations: erg-vikoke (invocation logging via a shared shim — also arms siciri's declined-verdict tripwire) and erg-kegiko (flag-surface audit; bon grew four flags tonight).
- bon-kafono (bon-prefix squatters) got more urgent in yesterday's correction and stayed untouched tonight — cross-repo surgery wanting its own session.

## For Claudes to come

**Before sealing a design adjudication, search the estate's same-day sessions for parallel thought.** Tonight's caller-mistake ladder was drafted solo and was *adequate*; one deja search ("Ronacher", Sameer's term) found a same-morning theory room and a same-afternoon 300k-call measurement that upgraded every verdict — rung 4 gained its repair-reporting rule, a decline gained a measurable tripwire, and the whole thing earned a doctrine home instead of a chat reply. The estate now regularly thinks the same thought in two rooms on one day; deja makes convergence checkable in one command, and the human's throwaway search term is often the best query. Related craft: **a negative-assertion test passes vacuously on empty output** — the dashboard banner test ("Session restarted" not in output) passed while the hook emitted nothing at all; only its siblings' positive assertions exposed it. Pair every "X not in output" with an assertion that output exists.
