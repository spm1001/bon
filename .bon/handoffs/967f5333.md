# Handoff — 2026-03-31 (auto)

session_id: 967f5333-547d-487d-bd74-50016bc026d3
purpose: Fix stale script-finding and auto-handoff quoting bugs, then start bon-gokofa (plugin instruction shards)

## Done
- **bon-venasi** resolved: `skills/close/SKILL.md` — renamed `SCRIPTS` → `BON_SCRIPTS` (6 refs) for consistency; fixed `GARDE_SCRIPTS` to use `ls -td` instead of `find | head -1`
- **bon-cahuwo** resolved: `scripts/auto-handoff.sh` — replaced `nohup bash -c` single-quote embedding with temp-script-file approach; LLM failure now falls back to mechanical handoff instead of silent nothing; 8 new tests in `tests/test_auto_handoff.py`
- **bon-gokofa** (partial): Created `instructions.md` (bon shard) — always-on gates only: GTD vocabulary, "bon not TodoWrite" override, mandatory `Skill(bon)` gate, capture/triage rules
- Updated `hooks/session-start.sh` to symlink `~/.claude/rules/bon.md → instructions.md` on every session start (both fresh and resume)
- 405 tests passing (397 pre-existing + 8 new)

## Next
- **Commit** all uncommitted changes — `skills/close/SKILL.md`, `scripts/auto-handoff.sh`, `hooks/session-start.sh`, `instructions.md`, `tests/test_auto_handoff.py`; version bump to 0.12.0
- **bon-gokofa**: Verify bon shard loads in a fresh session — `echo "What GTD vocabulary replaces 'Epic'?" | claude -p` should answer "Desired Outcome"
- **bon-gokofa**: Create shards for batterie, passe, trousse, mise (each ~3–10 lines of always-on gates/overrides only — skill headers carry the rest)
- **bon-gokofa**: Add SessionStart hooks to trousse, passe, mise, garde-manger to symlink their own shards
- **bon-gokofa**: Slim global `~/.claude/CLAUDE.md` — remove content now in shards

## Gotchas
- Auto-generated handoff — no reflective close was performed
- **Nothing is committed** — all session work is unstaged/untracked; `.bon/contributions/2026-03-31T072500.md` is deleted but not committed
- bon-gokofa is only partially done — bon shard exists and hook is wired, but no other plugin shards exist and global CLAUDE.md has not been slimmed yet
- The hook symlinks to the source repo path during development; in production it'll resolve to the cache path (self-heals on plugin update)
- Decision made: each plugin owns its own SessionStart hook + shard (option 1), not a batterie umbrella hook
