# Handoff — 2026-03-31 (auto)

session_id: c3451de4-be39-4544-923f-404f13fd2d6f
purpose: Add `--how` field to bon briefs across the full stack and restructure the /bon skill

## Done
- Verified skill routing (bon-fobewu): 0.10.0 cache loads `skills/bon/SKILL.md` correctly, rename resolved
- Verified bon-bohaki: `close-context.sh` walk-up logic was already correct in 0.10.0 cache
- Redesigned bon-tufihu: `--how` as optional inline prose field (not linked document); plan-to-bon transmutation makes bon self-contained
- bon-wuzotu: Added `--how` to CLI — `cli.py` (`cmd_new`, `cmd_edit`, `prompt_brief`, `require_brief_flags`, argparse), `storage.py` validation untouched (optional field)
- bon-sukave: `bon show` displays `how` between why/what; `bon work` prints "Approach:" above steps; JSON output normalised via `_normalize_brief()` helper in `display.py`
- bon-vakuki: `bon-read.sh` updated to display `how` in current/all modes; tactical hook verified safe (doesn't touch brief)
- Fixed edit clearing bug: `args.how is not None` check so `--how ""` actually clears the field
- Added `OPTIONAL_BRIEF_FIELDS` dict in `display.py` for consistent JSON normalisation across all output paths
- bon-livolo: 16 new tests in `tests/test_how.py`; fixed interactive test mock sequences (why/how/what/done order); all 397 tests passing
- Updated `SPEC.md` with `--how` field and brief schema
- Fixed `/close` skill stale-script-finder: changed `find ... | head -1` to `ls -td ... | head -1` so newest cached version always wins (addresses bon-venasi)
- bon-tosito: Rewrote `skills/bon/SKILL.md` 317→249 lines — identity crisis resolved, plan-to-bon transmutation section added, `--how` throughout, dead Session Close Protocol section removed
- Updated `CLAUDE.md` and `understanding.md` to document `--how` field
- Bumped version to 0.11.0; confirmed `/batterie:batterie-update` handles full marketplace + plugin update end-to-end
- Closed bon-tufihu (all 5 actions complete)

## Next
- bon-venasi: stale script finder fix is in the skill source but validate it's in the cache after the 0.11.0 update takes hold; may need to verify `ls -td` actually picks newest version under real multi-version layout
- bon-cahuwo: auto-handoff nohup quoting fragility — use temp script file instead of `bash -c` embedding
- bon-gokofa: instruction shards via `~/.claude/rules/` symlinks — parked, depends on skill stability (now achieved)
- Consider making `--how` mandatory: one-line change in `validate_item()` + `require_brief_flags()`, migration is `bon edit ID --how "..."` on existing items; `OPTIONAL_BRIEF_FIELDS` in `display.py` is the single toggle point

## Gotchas
- Auto-generated handoff — no reflective close was performed
- bon-bohaki was closed prematurely: the `close-context.sh` *content* was fixed but the `/close` skill's find pattern was still picking old-version scripts; the actual fix landed in bon-venasi work this session
- bon-tufihu was originally scoped as a linked-document design (`.how.md` file); the approach was changed this session to inline prose field — if you see old linked-document references in history, they're superseded
- `/tmp/bon-how-test` temp directory left behind from smoke testing; safe to delete
- `bon list --json` and `bon show --json` both go through `_normalize_brief()` now — any consumer expecting absent `how` key will now see `"how": null`; this is intentional
