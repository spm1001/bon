# Handoff — 2026-03-21

session_id: 74e347f0-15ad-44c4-a1d5-75568b95e4da
purpose: Stood up Dolt backend — systemd service, cross-machine access, install ergonomics

## Done
- Dolt SQL server running as systemd user service (`dolt-bon.service`)
- Session-start hook detects `.bon/backend` — Dolt projects use CLI, connection failures show recovery command
- `migrate --to dolt` renames stale JSONL files to `.pre-dolt`
- Migrated bon repo itself to Dolt backend (b796f19)
- Secured DB: dropped root, scoped `bon` user with wildcard host grant
- Install scripts (setup.sh, update-all.sh, ensure-bon.sh) include `[dolt]` extras for pymysql
- Cross-machine tested: Mac reading from Dolt on hezza via Tailscale
- Cleaned test debris (dtest-*, dhook-* prefixes)
- README documents full Dolt setup including systemd, user creation, config
- CHANGELOG updated for 0.8.1, version bumped

## Gotchas
- Dolt binds to 0.0.0.0 — relies on Tailscale network isolation, not bind address
- bon user has empty password — acceptable for Tailscale-only, but if the machine ever gets a public IP, lock this down
- `dolt sql` local CLI bypasses server auth entirely (runs in-process against data dir)
- Dolt 1.83.6 removed `--user`/`--password` flags from `sql-server` — users must be created via SQL
- batterie-update updates plugins but NOT repos — `git pull` is still needed before working on a repo changed elsewhere
- hezza's dolt.toml uses Tailscale IP (100.118.68.100), not 127.0.0.1
- Mac has a local edit to skills/close/SKILL.md that was stash-popped — check if it matters

## Risks
- No auth beyond "are you on the Tailscale network" — a compromised tailnet peer could write to bon
- Reverse migration (`bon migrate --to jsonl`) untested with `.pre-dolt` files present
- Plugin marketplace installs don't auto-install CLI tools — ensure-bon.sh only advises

## Next
- Test reverse migration path (jsonl round-trip)
- Consider adding `bon doctor` check for Dolt connectivity when backend=dolt
- Multi-machine tactical steps — does `bon step` from Mac correctly use hostname-prefixed session identity?

## Commands
```bash
# Check Dolt service
systemctl --user status dolt-bon.service

# Check cross-machine connectivity
bon list  # from any machine with dolt.toml

# Reverse migration if needed
bon migrate --to jsonl
```

## Reflection
**Claude observed:** The gap between "backend code exists" and "backend is operational" was larger than expected — Dolt identity config, systemd, user grants, bind addresses, pymysql in install paths, hook awareness. The session-start hook's file-existence check was a silent failure mode that would have confused every future Dolt user. The cross-machine test immediately exposed the "git pull is still needed" reality — the backend config travels via git, not via Dolt itself.
**User noted:** The chicken-and-egg of needing git pull to discover the Dolt backend is acceptable — you should be on fresh code anyway. A separate backends.toml would just be more state to track. The root@ exposure was flagged as a real concern. pymysql needs to be baked into the install path, not an afterthought.
