# Handoff — 2026-07-08

session_id: 8a6f57e1-a685-4473-a038-53113de8aa6d
purpose: Ship bon-loguvu (6 review-skill improvements) + kuhati/gasase; clear the orphan-prefix backlog; fix publish-skill doc drift
format: fond-v1

## For the next Claude

### Done
- **Shipped bon-loguvu (suite 1.5.1).** `audit_survey.py`: emits `visibility_note` + `dolt_open`/`jsonl_open` (the clone-dependent-count caveat — 491↔650 reads as clones appearing, not work); passes `how` through (was silently dropped, 0/650 → 436 items); derives `open_child_count` on outcomes (the kegewe signal, 70 outcomes). Review `SKILL.md`: **EXTERNAL_SURFACE** class (done-criteria off-repo, distinct from UNCLEAR), **rolling dispatch** (refill-on-completion), **TRUTH/DESIRE Phase 3** (verify truth mechanically; hold the desire conversation at repo/outcome level before item tables), **orphan-close recipe** (Phase 4). Verified live against real Dolt. Closed bon-loguvu.
- **Cleared the entire orphan-prefix backlog.** Closed 14 items via the new recipe — consomme 4, gm 4, day 4 (done); tmig 2 (archived, test litter). All seven orphan prefixes (consomme/day/gm/tmig/arc/sl/work) now 0 open. Scratch `repos` rows DELETEd + dolt-committed. Filed **bon-wezahu** (`bon --board <prefix>`, tripwire-gated — build when the recipe/ssh dance grates).
- **Shipped kuhati + gasase (suite 1.5.2).** kuhati = Phase 2 **freshness gate** (`git fetch` + `rev-list HEAD..@{u}`; ff-pull if clean+behind, flag "verdicts provisional" if not — closes the stale-clone false-green gap). gasase = fixed `skills/open/SKILL.md`'s broken heredoc reading example (`| python3 <<EOF … json.load(sys.stdin)` → `python3 -c`; verified both directions live — old form dies `JSONDecodeError: line 1 column 1`). Swept: close skill already correct, instructions.md/CLAUDE.md clean. Closed both.
- **Shipped publish-skill doc fix (suite 1.5.3).** "Publishing is a hezza operation" → "any host with `~/repos`+`gh` — tube is primary since the turndown." `publish.py`'s UTC/BST handling was already correct (`calendar.timegm`, comment documents the 2026-07-06 tube fix) — left intact.
- **understanding.md:** folded the two-axes review knowledge (from handoff synthesis at /open), trued the operational-lore paragraph to shipped 1.5.1 state, and marked the orphan backlog cleared.

### Reflection
- Three clean suite bumps in one session (1.5.1 / 1.5.2 / 1.5.3), all assemble-green (~14–22s), zero quarantines. **Publishing from tube works end-to-end** — gh push to spm1001 + CI dispatch + pull — confirming tube as the publish host post-turndown (the doc now says so).
- loguvu's point 2 brief was imprecise: "strips parent/waiting_for/how" — but the actual 2026-07-08 survey JSON carried `parent` (336) and `waiting_for` (35); only `how` was missing (0/650). Checking the artefact reshaped the fix (add `how` + derive `open_child_count`, not "add 3 fields") and stopped me "fixing" what wasn't broken. Field-report-remedy-ages, at fix-list grain.
- Sameer steered the orphan mechanism to recipe-now over building the flag: the flag's real justification is ssh-elimination for multi-machine reviews, but that pain is unproven (we *deferred* rather than ssh'd yesterday). Veil-with-tripwire → wezahu.
- consomme validation tasks closed as **moot** (skill lives in trousse, months-proven), not moved to trousse — Sameer's desire call, recorded in the close notes.

### Risks
- Restart needed to activate the shipped skill/hook changes (SessionStart hooks fire only on full restart; CLI reinstall is already live).
- EXTERNAL_SURFACE / open_child_count / freshness-gate are shipped but **unexercised** — first real test is the next `/review`. Watch EXTERNAL_SURFACE calibration (subagents mustn't over-apply it to code-verifiable items).
- The orphan-close recipe leaves scratch `repos` rows unless the DELETE step runs (harmless — invisible for 0-open boards — but untidy). wezahu is the permanent fix.
- kuhati's freshness gate is a SKILL.md *instruction*, not a helper — a rushed reviewer could skip the fetch pass. A `clone_freshness.py` helper is the hardening if that happens in practice (Chill'd for now).

### Opportunities
- **bon-suvise** (close-context.sh resolves HANDOFF_DIR into a *marketplace cache* when the cwd repo has no `.bon`) + **bon-robuju** (riffing handoffs mis-routed by the most-recent-commit heuristic) — the close-context.sh scan-down bug pair; suvise is tight, robuju more open-ended design.
- **bon-razonu** — an `area` field + grouped `bon list`; a proper feature across both backends, the next real bon-native build.
- **bon-mevifu** — remaining JSONL boards → Dolt or deliberately kept (estate-wide; touches other repos).
- **bon-wezahu** — the `--board` flag; build when the orphan/ssh dance actually grates.
- The docket/rite roadmap (kepuko / welogi / kuwivo / gopewu) remains the strategic spine.

## For Claudes to come

The review skill is now demonstrably **self-improving**: each big estate run yields a fold-the-learnings bon (bon-gokabi after run 1, bon-loguvu after run 2), and this round shipped that round's learnings *into* the skill — so the next reviewer inherits the machinery, not the rediscovery. Two durable mechanics worth carrying. (1) Verification's real failure mode is a **confident wrong verdict from a stale clone** — worse than NOT_VERIFIABLE_HERE, because it looks identical to a fresh verdict; hence the freshness gate fetches-and-flags *before* dispatch, and a clone that can't fast-forward yields provisional verdicts, not auto-closable ones. (2) A "fold the learnings" brief is itself subject to *a field report's remedy ages faster than its problem*: check each point against current code before implementing — loguvu point 2 was two-thirds already done (parent/waiting_for flowed; only `how` was missing), and building it literally would have "fixed" what wasn't broken. The ground truth for a survey-behaviour brief is the survey's actual JSON output, not the brief's prose.
