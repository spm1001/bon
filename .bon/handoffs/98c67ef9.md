# Handoff — 2026-03-31

session_id: 98c67ef9-cb81-4228-9818-446ef199ef2b
purpose: JSON stdin as default input for bon new, narrated walkthrough, plugin hook hardening

## Done
- Implemented `bon new --json` reading structured JSON from stdin (bon-tumira, closed)
- Made JSON-from-stdin the **default** — no flag needed when stdin is piped and no title given
- Wrote narrated system walkthrough replacing 13 stale snapshot files (bon-gudiku, closed)
- Added PostToolUse hook on ExitPlanMode ("plans become bons" reminder)
- Switched auto-handoff background model from Haiku to Opus
- Added ephemeral HOME pattern to auto-handoff (borrowed from ardoise) so background sessions don't pollute `claude -r`
- Fixed SessionStart:resume hook error — removed `set -euo pipefail`, individual failure handling
- Merged instruction shard symlink into `ensure-bon.sh` (one hook for CLI check + shard)
- Fixed YAML frontmatter in all 3 SKILL.md files (comma-separated allowed-tools → proper list)
- Updated CLAUDE.md with `bon new` input modes documentation
- Closed bon-nufica (already done in prior session)
- Ran 4 iterations of skill evals (12 subagent runs) testing `--json` adoption
- Version 0.14.1 → 0.15.0

## Gotchas
- Skill evals conclusively show Claude's flag prior is bedrock — 0/12 runs used JSON stdin despite increasingly forceful skill guidance. The CLI default change is the real fix; skill prose can't override procedural CLI instincts.
- Ephemeral HOME auto-handoff is untested on a real session exit — pattern borrowed from ardoise but needs live verification. Check `claude -r` after next session exit.
- `open-skill-workspace/` in repo root is gitignored but contains eval artifacts from 12 runs — can be deleted anytime.
- Auto-handoff now uses Opus, not Haiku — cost increase per session exit.

## Risks
- Implicit JSON detection (`not args.title and not sys.stdin.isatty()`) could surprise users who pipe non-JSON to `bon new` — they'd get "Invalid JSON" instead of "Title is required"
- SPEC.md drift widened — now describes neither `--json` flag nor implicit stdin detection (bon-fekatu filed)

## Next
- bon-fekatu: Update SPEC.md for JSON stdin and implicit detection
- bon-zamezi: Update README.md to show JSON stdin as default
- Verify ephemeral HOME auto-handoff works: exit a session, check `claude -r` doesn't show it, check `.bon/handoffs/` does have the file
- Consider PreToolUse hook on Bash matching `bon new` without piped stdin as future enforcement lever

## Reflection
**Claude observed:** The skill eval loop was the most illuminating part of this session. Four iterations of increasingly forceful prose — "prefer", "no exceptions", "never use flags", structural reordering — all failed to change Claude's mechanical choice of flags vs JSON stdin. The fix was changing the CLI default, not the instructions. Skills are great for teaching *what* to produce (brief quality was dramatically better in every with-skill run) but can't override *how* to invoke a CLI tool. That's a training prior, not a context problem.
**User noted:** "make json the default and make the flag the escape hatch" — the right instinct. Don't fight the prior; change the default so the prior doesn't matter.
