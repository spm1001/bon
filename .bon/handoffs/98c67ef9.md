# Handoff — 2026-03-31

session_id: 98c67ef9-cb81-4228-9818-446ef199ef2b
purpose: JSON stdin as default input for bon new, narrated walkthrough, plugin hook hardening

## Done
- Implemented `bon new --json` then made JSON-from-stdin the **default** (no flag needed when stdin is piped)
- Wrote narrated system walkthrough replacing 13 stale snapshot files (bon-gudiku, closed)
- Added PostToolUse hook on ExitPlanMode ("plans become bons" reminder)
- Switched auto-handoff from Haiku to Opus
- Added ephemeral HOME to auto-handoff — **confirmed working**: session exited, handoff written, `claude -r` clean
- Fixed SessionStart:resume hook error (removed `set -euo pipefail`)
- Merged instruction shard symlink into ensure-bon.sh
- Fixed YAML frontmatter in all 3 SKILL.md files
- Updated CLAUDE.md with `bon new` input modes documentation
- Ran 4 iterations of skill evals (12 subagent runs) — skill prose cannot override Claude's CLI flag prior
- Versions shipped: 0.13.0, 0.14.0, 0.14.1, 0.15.0

## Gotchas
- Resumed sessions bind to the plugin version from session creation — our fix is in 0.14.1+ but any session started on an older version will still see the resume hook error. Nothing we can do; Claude Code behaviour.
- Ephemeral HOME auto-handoff confirmed working but only tested once. Pattern borrowed from ardoise.
- `open-skill-workspace/` in repo root is gitignored eval artifacts — safe to delete.

## Risks
- Implicit JSON detection could surprise users who pipe non-JSON to `bon new`
- SPEC.md drift widened (bon-fekatu filed)

## Next
- **Bulk migration of bons from all repos to Dolt** — user's stated next priority
- bon-fekatu: Update SPEC.md for JSON stdin and implicit detection
- bon-zamezi: Update README.md to show JSON stdin as default

## Reflection
**Claude observed:** The skill eval loop was the session's most illuminating work. Four iterations of increasingly forceful prose all failed to change Claude's mechanical invocation pattern. The fix was changing the CLI default — don't fight the prior, change the tool. Skills teach *what* to produce brilliantly but can't override *how* to invoke a CLI.
**User noted:** "make json the default and make the flag the escape hatch" — the right instinct, applied immediately. Also: resumed sessions keeping old plugin bindings is painful but not our problem.
