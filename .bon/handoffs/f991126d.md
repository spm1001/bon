# Handoff — 2026-03-31

session_id: f991126d-5a19-4cbd-b965-6729e481603e
purpose: Merged /open + /tracker into single /bon skill, added progress indicators

## Done
- Processed two pending contributions into understanding.md (auto-handoff quoting fragility, Suggested/bon-list redundancy)
- Merged /open and /tracker skills into single /bon skill (skills/bon/SKILL.md, 317 lines)
- Added progress indicators to bon-read.sh — outcomes now show [3✓/5] counts in list output
- Updated understanding.md: fixed skills layer description, added auto-handoff landmine, added Suggested guidance
- Updated global CLAUDE.md: "at session start and before bon CLI commands"
- Updated project CLAUDE.md: skills/tracker → skills/bon path references
- Version bumped to 0.10.0
- Filed bon-bohaki (close-context.sh legacy handoff path) and bon-fobewu (verify skill routing after rename)

## Gotchas
- Plugin cache is still 0.9.0 — this session's /close skill loaded from 0.8.3, /tracker from 0.9.0. The new merged /bon skill won't be in cache until push + plugin update + restart
- close-context.sh still outputs HANDOFF_DIR to legacy ~/.claude/handoffs/ path (bon-bohaki filed)
- manifest.json (batterie-de-savoir orchestrator) has stale skill mappings: bon→skills/tracker and open→skills/open. This is a cross-repo concern — needs updating in batterie-de-savoir, not just bon
- The 0.8.3 → 0.9.0 → 0.10.0 cache lag is a recurring pattern (third consecutive session hitting it)

## Risks
- skills/tracker/ → skills/bon/ rename may break plugin skill routing if the system uses directory names rather than frontmatter name field (bon-fobewu filed)
- The merged skill is untested in a fresh session — first load will be the real test

## Next
- bon-fobewu: Push 0.10.0, update plugin, verify skill routing in fresh session — do this FIRST
- bon-bohaki: Fix close-context.sh handoff path to use .bon/handoffs/
- bon-gokofa: Plugin instruction shards via ~/.claude/rules/ symlinks (WS5)
- bon-cahuwo: Harden auto-handoff quoting and add silent-failure fallback
- Update manifest.json skill mappings (bon→skills/bon, remove open) — batterie-de-savoir repo concern
- bon-tufihu: Linked briefs for implementation choreography (parked)

## Commands
```bash
# Push and update
git push && claude plugin marketplace update batterie-de-savoir && claude plugin update bon@batterie-de-savoir

# Verify new skill loaded (after restart)
# Should see "Session Start Ritual" section, not old "CLI Installation" opener
```

## Reflection
**Claude observed:** The session started with a concrete problem (orphaned contributions) and evolved into a structural question (why are /open and /tracker separate?). The user's instinct to question the separation was right — /tracker couldn't load itself, making the separation an illusion. The merge was the real win. The progress indicators were a quick, clean change. The recurring cache lag theme (three sessions now) suggests it's not a bug to fix but a rhythm to accommodate — skills load from cache, code deploys update the cache, the gap is inherent.
**User noted:** "it's a mess but we're cleaning it I guess" — pragmatic acceptance of accumulated debt from rapid iteration. Caught the skills/tracker directory name issue ("surely that goes?") that I'd missed.
