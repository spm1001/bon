# Handoff — 2026-07-15

session_id: 560fe166-f392-4956-8711-71a8aba3953e
purpose: Proved candidate-mode end-to-end (live escape + mechanical mint); shipped gueridon + bon-read.sh cewemo fixes; scoped the Cowork read/write gap
format: fond-v1

## For the next Claude

### Done
- **/open synthesis** — integrated the candidate-mode-shipped + escape-path + convention-leaks knowledge into understanding.md (the "pending" framing was stale).
- **Proved candidate mode's mechanics end-to-end (pujawo/camoru)**:
  - *Escape leg — LIVE*: a Cowork `/close` on `~/notes` (run on tube's Desktop app) took the candidate path, notes-sync carried the untracked handoff off tube within ~1 min (commit `e18610ed`, ~15:19, ~1 min after the 15:18 write), correctly placed in `work/scaled-measurement/lantern/handoffs/`. It correctly produced **zero candidates** (work-content → Todoist/room, not the board).
  - *Mint leg — MECHANICAL*: synthetic scratch board in `/tmp` (removed after) — all three verbs minted (NEW with provenance in `--why`, DONE with provenance in `done_note`, EDIT replaced `--how`), and the `### Candidates (minted YYYY-MM-DD)` marker gives a clean no-double-mint (0 unmarked blocks on re-open).
- **gueridon cewemo — DONE, committed `dd38234`, pushed, DEPLOY HELD**: `getLatestHandoff` now resolves visible `handoffs/` before `.bon/`, and ranks the newest across all dirs by **header date** (mtime tie-break) — full parity with `lib-handoff.sh`/`open-context.sh`, fixing the tube fresh-clone mtime bug. Red-then-green on 5 tests; full suite **767 green**.
- **bon-read.sh cewemo (G1) — DONE + published**: the JSONL raw-read path was blind to standalone actions (`list`/`ready` walked outcomes→children only), so a board whose open work is all standalone — like `~/notes` — read as **empty**, hiding all 7 items (Cowork Claude review). Added a "Standalone:" section to both; +4 tests red-then-green; **526 bon tests green**; verified against the live notes board (all 7 now surface). Shipped via `/batterie:publish`.
- **cewemo updated**: compost dropped (marmite doesn't resolve handoff dirs — reads `~/notes/raw/**`), mtime folded in, gueridon marked DONE.
- **Filed**: `bon-donunu` (/open host-aware), `bon-jucuru` (bon-write.sh + guardrail), `bon-taviga` (script discovery on PATH), `bon-renili` (candidate-mode rite hardening — Findings A+B).

### Reflection
- The **Cowork Claude's review was a hostile-review-in-a-curious-costume** — it found the read/orient-mirror gap I'd missed: `/close` got the Board/Writer matrix but `/open` didn't, and `bon-read.sh` was standalone-blind. Verified **both against the code** before acting (not on the report alone) — both confirmed.
- **Read the canonical reader before mirroring it**: my first gueridon visible-first test encoded the wrong semantic (same-date fixtures made "visible-first" look like a reader rule); reading `open-context.sh` showed the reader ranks by *header date*, and visible-first is emergent from the *writer* + date-ranking. The reference impl is the spec.
- Verify-to-failure held throughout: every test was confirmed red against the old code before trusting green; the mint proof used a throwaway board; the escape leg was verified against the file on disk, not the Cowork summary.

### Risks
- **gueridon DEPLOY HELD** — the dashboard still runs old mtime ranking until Sameer reinstalls `/opt/gueridon` + restarts (which bounces live phone-launched sessions). Don't assume it's live; `git log` shows `dd38234` pushed but not deployed.
- **aboyeur cewemo half is assigned to another Claude** — it needs the spec (`bon show bon-cewemo`): inline the visible-first walk in `conductor.sh:latest_handoff_has_escalation` (no bon dependency), rank by header date not `ls -t`. Coordination risk if two sessions touch it.
- **bon-jucuru (bon-write.sh) touches the no-delete, autosyncing notes mount** — the live-board write guardrail is load-bearing: a malformed/dup hand-write is stuck and pushed within ~5 min. Candidate-mode must stay the default; direct write opt-in.

### Opportunities
- **pujawo + camoru**: mechanics proven end-to-end; hold open on the one remaining leg — an **organic** candidate-bearing round-trip (a real Cowork session doing genuine notes-*board* work → real `### Candidates` → real mint). Rarely hit by accident (the surface tension in understanding.md), so it needs a deliberate notes-board action in Cowork.
- **bon-cewemo**: gueridon DONE (deploy held); **aboyeur remains** (other Claude).
- **gopewu read/write cluster**: `bon-donunu` (/open host-aware — cousin of `bon-nujucu`), `bon-jucuru` (bon-write.sh), `bon-taviga` (discovery). The Cowork Claude offered to draft the bon-write.sh sketch.
- **bon-renili**: candidate-mode rite hardening (script-less `/close` context fallback; explicit `/open` skip-on-minted).
- **gueridon deploy** — Sameer's hand.

## For Claudes to come

Already synthesized into understanding.md this session (docket/rite + handoff-resolution sections) — flagged here so the next `/open` doesn't double-integrate. The keystone: **candidate mode's two halves are hard to exercise on one surface** — the auto-syncing surface (`~/notes`) diverts most work to Todoist/room-handoffs (few candidates), while the surfaces that naturally *produce* candidates (a code repo doing board work it can't write) don't auto-sync. So proving such a rite splits cleanly into *mechanical* (a synthetic fixture proves the logic) and *organic* (a real session proves the integration); the mechanical proof is legitimate and de-risks the wait for the organic one. Corollary now in the codebase: handoff **consumers must mirror the reader's *ranking* (header date), not just its dirs** — "visible-first" alone is the wrong semantic.
