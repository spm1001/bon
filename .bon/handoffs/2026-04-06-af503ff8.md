# Handoff — 2026-04-06

session_id: af503ff8-ccd1-48a6-a7ed-763e1723af1a
purpose: Fix session-start hook — handoff was buried behind understanding.md in truncated output
format: fond-v1

## For the next Claude

### Done
- Diagnosed why "For Claudes to come" was missed at session start — hook output was 25.3KB, understanding.md (~7KB) came first, handoff was past the 2KB preview truncation
- Removed understanding.md from `open-context.sh` output — the /open skill reads it explicitly during synthesis, so the hook copy was redundant
- Added truncation recovery guidance to /open SKILL.md — tells Claude to Read the full hook output file when preview is truncated
- Integrated migration blast radius lesson from e60ffdb9 handoff into understanding.md
- Two version bumps: 0.19.2 (hook fix), 0.19.3 (skill fix), both pushed and deployed through full pipeline

### Reflection
This was a clean diagnostic session. The failure mode was subtle — the workflow *was* producing the right output, but the output ordering meant the most important part (the handoff) was invisible after truncation. The fix is structural: put dynamic content first, stable reference later. The /open skill fix is belt-and-suspenders — even if truncation happens again, the Claude now knows to read the full file.

Sameer caught this because he noticed the "For Claudes to come" section should have existed. Without that instinct, the failure would have been silent — each new Claude would have skipped synthesis, and the handoff knowledge pipeline would have quietly stopped flowing.

### Risks
- The plugin cache now has 0.19.1, 0.19.2, and 0.19.3. Old versions are inert but accumulate disk space. Not urgent.
- understanding.md is still ~130 lines. It works but carries more than it needs to. Sameer opted to leave this for now.

### Opportunities
- bon-dehapo — Mixed handoff filename formats (legacy UUID vs fond-v1 date-prefixed) caused the manual lookup error. Today's incident confirms this isn't theoretical.
- understanding.md sharpening — cut from ~130 to ~60 lines, keeping only what the code won't tell you. Sameer acknowledged the assessment but deferred.
- bon-pecake — Dolt backup to GitHub, still the last open action under bon-wiwine.

## For Claudes to come

Automated context injection (hooks, session-start scripts) should front-load what's *different* this session — handoffs, tactical state, recent changes — not what's *true* about the project. Stable reference (architecture, data model, design philosophy) belongs in explicit Read steps triggered by skill instructions, not in the stream. When the stream is truncated (and it will be — understanding.md alone was 7KB), the dynamic content is what gets lost, and that's the content with no other delivery path. The principle: hook output decays, skill-driven reads persist. Design accordingly.
