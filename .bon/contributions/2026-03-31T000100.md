The tracker skill tells Claude to run `bon list` at session start, but the enriched hook briefing (0.9.0) already provides a curated Suggested section from the previous handoff's Next items. Running `bon list` after seeing Suggested is redundant for the "what next?" question — it burns tokens re-fetching what's already in context. The Suggested section should be treated as authoritative for direction-picking. `bon list` is for when you need the full picture: hierarchy, status markers, mid-session transitions, or auditing completeness. The tracker skill needs updating to say: "if Suggested items are in context from the hook, trust them — don't re-run bon list to verify."